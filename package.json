{
  "name": "codex-project-gallery",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/project-utils.test.mjs",
    "validate": "node scripts/validate-projects.mjs",
    "verify": "npm run validate && npm test"
  }
}
